<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Time_Finder</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- The test project lives in a subfolder; keep its files out of this project. -->
  <ItemGroup>
    <Compile Remove="Time-Finder.Tests/**" />
    <None Remove="Time-Finder.Tests/**" />
  </ItemGroup>

</Project>
